# Ruff 基线配置：只启用致命错误类检查（语法错误、未定义名称等），
# 不做风格检查，避免对存量代码大规模改动。后续可逐步收紧规则。
target-version = "py39"
line-length = 120

extend-exclude = [
    ".venv",
    "frontend",
    "reports",
    "uploads",
    "outputs",
]

[lint]
# E9: 语法/缩进错误；F63: 无效比较；F7: 语法级误用；F82: 未定义名称
select = ["E9", "F63", "F7", "F82"]
